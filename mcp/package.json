{
  "name": "slopos-mcp-shell",
  "version": "0.1.0",
  "description": "MCP server that lets AI agents run SlopOS shell commands.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.1",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  }
}
