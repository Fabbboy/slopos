[package]
name = "slopos-alloc"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
# No external dependencies — slopos-alloc is the kernel's in-house
# allocation + in-place-init surface. The `Init<T, E>` / `Zeroable`
# surface lives in `src/init.rs`; see that module's header for the
# design rationale (no `Pin` machinery, no macro-generated closure
# scratch).
