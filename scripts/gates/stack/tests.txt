# check_stack_sizes allowlist — variant: tests
#
# Frames above STACK_SIZE_THRESHOLD (2048 B) that are permitted, each capped
# at its measured size so growth still fails.
#
#     scripts/check_stack_sizes.sh --variant tests --emit-allowlist builddir/kernel-tests.elf
#
# Every entry must match at least one over-threshold frame; a dead exemption
# fails the gate. No entry may exceed the 4096 B guard page.
#
# There are deliberately no entries for test functions. Test code runs on the
# same 32 KiB task stacks as everything else, so it lives under the same
# ceiling; a frame that exceeds it is refactored, not exempted.

# Measured 2026-07-31: 44732 records. See the dev allowlist for why the
# floors are set relative to the other variants' counts.
min-records 30000

# The kernel/tests image registers thousands of tests; a dev or release ELF
# trips here.
expect-test-registry many

# <max-bytes> <TAB> <glob over the rustc-mangled symbol>

# Vendored-unwinder frames: reached only while a panic unwinds, never on a
# hot path. The DWARF CFI evaluation is inherently frame-heavy.
2216	*9unwinding8unwinder8find_fde16gnu_eh_frame_hdr*find_fde*
3864	*CallFrameInstruction*E5parse*9unwinding*
2744	*unwinding9panicking12catch_unwind8do_catch*slopos_ostd6unwind11KernelPanic*
2616	*unwinding8unwinder5frame*Frame12from_context*
2088	*unwinding8unwinder*__Unwind_Backtrace*
2072	*unwinding8unwinder19force_unwind_phase2*
2072	*UnwindTable*StoreInVec*E8evaluate*

# SlopOS functions whose large temporaries are already heap-boxed; the
# residual frame is inherent local/format-args state, still under the guard
# page.
2424	*slopos_acpi3aml14process_device*
2344	*slopos_net4ipv49handle_rx*
2200	*slopos_net3dns11dns_resolve*
2200	*slopos_acpi3aml6interp*Interp4eval*
2216	*slopos_core7syscall2fs19poll_ioctl_handlers14syscall_select*
2072	*slopos_core7syscall16process_handlers18syscall_spawn_path*
2312	*slopos_core7syscall5tests41test_signal_install_deliver_and_sigreturn*
2248	*slopos_core7syscall5tests33test_unix_socket_poll_syscall_e2e*
2184	*slopos_core4exec5utest8dispatch*
2104	*slopos_core4exec16setup_user_stack*
2072	*slopos_core7syscall5tests44test_sigprocmask_block_then_unblock_delivery*
2072	*slopos_mm10process_vm24process_vm_clone_cow_for*

# Library smallsort monomorphizations: scratch is the element size, not
# reducible without changing the sorted element type.
2248	*sort4_stable*slopos_drivers3pci*MatchIndex14candidates_for*
2248	*sort4_stable*slopos_drivers12platform_bus9matchmake*
